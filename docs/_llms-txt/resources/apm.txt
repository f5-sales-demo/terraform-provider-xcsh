# f5xc_apm

Category: Monitoring
New APM as a service with configured parameters

## Required

- name
- namespace

## Minimal Valid Config

resource "f5xc_apm" "example" {
  name      = "example-apm"
  namespace = "staging"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  aws_site_type_choice {
  }
  apm_aws_site {
  }
  admin_password {
  }
}

## Dependencies

Requires: none
