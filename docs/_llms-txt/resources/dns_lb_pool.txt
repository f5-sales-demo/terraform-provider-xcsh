# xcsh_dns_lb_pool

Category: DNS
DNS Load Balancer Pool in a given namespace. If one already exist it will give a error

## Required

- name

## Minimal Valid Config

```terraform
resource "xcsh_dns_lb_pool" "example" {
  name      = "example-dns-lb-pool"
  namespace = "system"
}
```

## Dependencies

Requires: none
