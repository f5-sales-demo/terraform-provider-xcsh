# f5xc_k8s_pod_security_admission

Category: Kubernetes
K8s_pod_security_admission will create the object in the storage backend

## Required

- name
- namespace

## Minimal Valid Config

```terraform
resource "f5xc_k8s_pod_security_admission" "example" {
  name      = "example"
  namespace = "system"

  labels = {
    "env" = "production"
  }
}
```

## Dependencies

Requires: none
