# xcsh_dns_domain

Category: DNS
DNS Domain in a given namespace. If one already exist it will give a error

## Required

- name
- namespace
- dnssec_mode

## Minimal Valid Config

```terraform
resource "xcsh_dns_domain" "example" {
  name      = "example-dns-domain"
  namespace = "staging"

  dnssec_mode = "DNSSEC_DISABLE"
}
```

## Dependencies

Requires: none
