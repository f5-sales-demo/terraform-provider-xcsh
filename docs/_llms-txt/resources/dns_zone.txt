# xcsh_dns_zone

Category: DNS
DNS Zone in a given namespace. If one already exist it will give a error

## Required

- name
- namespace

## Minimal Valid Config

```terraform
resource "xcsh_dns_zone" "example" {
  name      = "example-dns-zone"
  namespace = "staging"
}
```

## Dependencies

Requires: none
