# f5xc_api_crawler

Category: API Security
API Crawler resource

## Required

- name
- namespace

## Minimal Valid Config

resource "f5xc_api_crawler" "example" {
  name      = "example-api-crawler"
  namespace = "staging"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  domains {
  }
  simple_login {
  }
  password {
  }
}

## Dependencies

Requires: none
