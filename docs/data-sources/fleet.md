---
page_title: "xcsh_fleet Data Source - xcsh"
subcategory: "Sites"
description: |-
  Retrieves information about fleet will create a fleet object in 'system' namespace of the user in F5 Distributed Cloud.
---

# xcsh_fleet (Data Source)

Retrieves information about fleet will create a fleet object in 'system' namespace of the user in F5 Distributed Cloud. This is a read-only data source.

~> **Note:** For more information, see the [F5 Distributed Cloud API documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Fleet Data Source Example

terraform {
  required_version = ">= 1.0"

  required_providers {
    xcsh = {
      source  = "f5-sales-demo/xcsh"
      version = ">= 0.1.0"
    }
  }
}

# Look up an existing Fleet by name
data "xcsh_fleet" "example" {
  name      = "example-fleet"
  namespace = "staging"
}

output "fleet_id" {
  value = data.xcsh_fleet.example.id
}
```

<!-- schema generated by tfplugindocs -->

## Argument Reference

-> **Syntax Rule:** This provider uses OneOf groups for mutually exclusive options. Fields documented as "Optional Block" use block syntax `field_name { ... }`. Empty OneOf object attributes use `field_name = {}`; conditional selection uses `condition ? {} : null`. Boolean attributes (such as `add_hsts` and `http_redirect`) use `= true` or `= false`.

🔶 **High Risk Operations** — Some operations on this resource have high danger level. Destructive operations may require confirmation.

### Metadata Argument Reference

<a id="name"></a>&#x2022; [`name`](#name) - Required String<br>Name of the Fleet

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Required String<br>Namespace where the Fleet exists

### Attributes Reference

In addition to all arguments above, the following attributes are exported:

<a id="allow-all-usb"></a>&#x2022; [`allow_all_usb`](#allow-all-usb) - Optional Object<br>Configuration parameter for allow all usb

<a id="annotations"></a>&#x2022; [`annotations`](#annotations) - Optional Map<br>Annotations applied to this resource

<a id="blocked-services"></a>&#x2022; [`blocked_services`](#blocked-services) - Optional List<br>Disable node local services on this site

<a id="bond-device-list"></a>&#x2022; [`bond_device_list`](#bond-device-list) - Optional String<br>Bond Devices List. List of bond devices for this fleet

<a id="dc-cluster-group"></a>&#x2022; [`dc_cluster_group`](#dc-cluster-group) - Optional String<br>Type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name

<a id="dc-cluster-group-inside"></a>&#x2022; [`dc_cluster_group_inside`](#dc-cluster-group-inside) - Optional String<br>Type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name

<a id="default-config"></a>&#x2022; [`default_config`](#default-config) - Optional Object<br>Enable this option

<a id="default-sriov-interface"></a>&#x2022; [`default_sriov_interface`](#default-sriov-interface) - Optional Object<br>Configuration parameter for default sriov interface

<a id="default-storage-class"></a>&#x2022; [`default_storage_class`](#default-storage-class) - Optional Object<br>Configuration parameter for default storage class

<a id="deny-all-usb"></a>&#x2022; [`deny_all_usb`](#deny-all-usb) - Optional Object<br>Configuration parameter for deny all usb

<a id="description"></a>&#x2022; [`description`](#description) - Optional String<br>Description of the Fleet

<a id="device-list"></a>&#x2022; [`device_list`](#device-list) - Optional String<br>Add device for all interfaces belonging to this fleet

<a id="disable-gpu"></a>&#x2022; [`disable_gpu`](#disable-gpu) - Optional Object<br>Configuration parameter for disable GPU

<a id="disable-log-anonymization"></a>&#x2022; [`disable_log_anonymization`](#disable-log-anonymization) - Optional Object<br>Configuration parameter for disable log anonymization

<a id="disable-vm"></a>&#x2022; [`disable_vm`](#disable-vm) - Optional Object<br>Enable this option

<a id="enable-default-fleet-config-download"></a>&#x2022; [`enable_default_fleet_config_download`](#enable-default-fleet-config-download) - Optional Bool<br>Enable default fleet config, It must be set for storage config and GPU config

<a id="enable-gpu"></a>&#x2022; [`enable_gpu`](#enable-gpu) - Optional Object<br>Enable this option

<a id="enable-log-anonymization"></a>&#x2022; [`enable_log_anonymization`](#enable-log-anonymization) - Optional Object<br>Configuration parameter for enable log anonymization

<a id="enable-vgpu"></a>&#x2022; [`enable_vgpu`](#enable-vgpu) - Optional String<br>Licensing configuration for NVIDIA vGPU

<a id="enable-vm"></a>&#x2022; [`enable_vm`](#enable-vm) - Optional Object<br>VM Configuration. VMs support configuration

<a id="fleet-label"></a>&#x2022; [`fleet_label`](#fleet-label) - Optional String<br>Fleet_label value is used to create known_label 'F5 XC/fleet=`<fleet_label>`' The known_label is created in the 'shared' namespace for the tenant. A virtual_site object with name `<fleet_label>` is also created in 'shared' namespace for tenant. The virtual_site object will select all sites

<a id="id"></a>&#x2022; [`id`](#id) - Optional String<br>Unique identifier for the resource

<a id="inside-virtual-network"></a>&#x2022; [`inside_virtual_network`](#inside-virtual-network) - Optional List<br>Default inside (site local) virtual network for the fleet

<a id="interface-list"></a>&#x2022; [`interface_list`](#interface-list) - Optional String<br>Add all interfaces belonging to this fleet

<a id="kubernetes-upgrade-drain"></a>&#x2022; [`kubernetes_upgrade_drain`](#kubernetes-upgrade-drain) - Optional String<br>Specify how worker nodes within a site will be upgraded

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>Labels applied to this resource

<a id="log-receiver"></a>&#x2022; [`log_receiver`](#log-receiver) - Optional String<br>Type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name

<a id="logs-streaming-disabled"></a>&#x2022; [`logs_streaming_disabled`](#logs-streaming-disabled) - Optional Object<br>Enable this option

<a id="network-connectors"></a>&#x2022; [`network_connectors`](#network-connectors) - Optional List<br>Network Connector defines connection between two virtual networks in a given site. Fleet defines one or more such network connectors. The network connectors configuration is applied on all sites that are member of the fleet

<a id="network-firewall"></a>&#x2022; [`network_firewall`](#network-firewall) - Optional List<br>Network Firewall defines firewall to be applied for the virtual networks in the fleet. The network firewall configuration is applied on all sites that are member of the fleet. Constraints The Network Firewall is applied on Virtual Networks of type site local network and site local inside network

<a id="no-bond-devices"></a>&#x2022; [`no_bond_devices`](#no-bond-devices) - Optional Object<br>Configuration parameter for no bond devices

<a id="no-dc-cluster-group"></a>&#x2022; [`no_dc_cluster_group`](#no-dc-cluster-group) - Optional Object<br>Enable this option

<a id="no-storage-device"></a>&#x2022; [`no_storage_device`](#no-storage-device) - Optional Object<br>Configuration parameter for no storage device

<a id="no-storage-interfaces"></a>&#x2022; [`no_storage_interfaces`](#no-storage-interfaces) - Optional Object<br>Configuration parameter for no storage interfaces

<a id="no-storage-static-routes"></a>&#x2022; [`no_storage_static_routes`](#no-storage-static-routes) - Optional Object<br>Configuration parameter for no storage static routes

<a id="operating-system-version"></a>&#x2022; [`operating_system_version`](#operating-system-version) - Optional String<br>Desired Operating System version that is applied to all sites that are member of the fleet. Current Operating System version can be overridden via site config

<a id="outside-virtual-network"></a>&#x2022; [`outside_virtual_network`](#outside-virtual-network) - Optional List<br>Default outside (site local) virtual network for the fleet

<a id="performance-enhancement-mode"></a>&#x2022; [`performance_enhancement_mode`](#performance-enhancement-mode) - Optional String<br>Optimize the site for L3 or L7 traffic processing. L7 optimized is the default

<a id="sriov-interfaces"></a>&#x2022; [`sriov_interfaces`](#sriov-interfaces) - Optional String<br>List of all custom SR-IOV interfaces configuration

<a id="storage-class-list"></a>&#x2022; [`storage_class_list`](#storage-class-list) - Optional String<br>Add additional custom storage classes in Kubernetes for this fleet

<a id="storage-device-list"></a>&#x2022; [`storage_device_list`](#storage-device-list) - Optional String<br>Add additional custom storage classes in Kubernetes for this fleet

<a id="storage-interface-list"></a>&#x2022; [`storage_interface_list`](#storage-interface-list) - Optional String<br>Add all interfaces belonging to this fleet

<a id="storage-static-routes"></a>&#x2022; [`storage_static_routes`](#storage-static-routes) - Optional String<br>Configuration parameter for storage static routes

<a id="usb-policy"></a>&#x2022; [`usb_policy`](#usb-policy) - Optional String<br>Type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name

<a id="volterra-software-version"></a>&#x2022; [`volterra_software_version`](#volterra-software-version) - Optional String<br>F5XC software version is human readable string matching released set of version components. The given software version is applied to all sites that are member of the fleet. Current software installed can be overridden via site config

<a id="dns"></a>&#x2022; [`dns`](#dns) - Optional Object<br>Enable this option

<a id="network-type"></a>&#x2022; [`network_type`](#network-type) - Optional String  Defaults to `VIRTUAL_NETWORK_SITE_LOCAL`<br>Possible values are `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`,
`VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, `VIRTUAL_NETWORK_SRV6_NETWORK`, `VIRTUAL_NETWORK_IP_FABRIC`, `VIRTUAL_NETWORK_SEGMENT`, `VIRTUAL_NETWORK_MANAGEMENT`<br>[Enum:
VIRTUAL_NETWORK_SITE_LOCAL|VIRTUAL_NETWORK_SITE_LOCAL_INSIDE|VIRTUAL_NETWORK_PER_SITE|VIRTUAL_NETWORK_PUBLIC|VIRTUAL_NETWORK_GLOBAL|VIRTUAL_NETWORK_SITE_SERVICE|VIRTUAL_NETWORK_VER_INTERNAL|VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE|VIRTUAL_NETWORK_IP_AUTO|VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK|VIRTUAL_NETWORK_SRV6_NETWORK|VIRTUAL_NETWORK_IP_FABRIC|VIRTUAL_NETWORK_SEGMENT|VIRTUAL_NETWORK_MANAGEMENT]
Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to

<a id="ssh"></a>&#x2022; [`ssh`](#ssh) - Optional Object<br>Enable this option

<a id="web-user-interface"></a>&#x2022; [`web_user_interface`](#web-user-interface) - Optional Object<br>Enable this option

<a id="bond-devices"></a>&#x2022; [`bond_devices`](#bond-devices) - Optional List<br>Bond Devices. List of bond devices

<a id="active-backup"></a>&#x2022; [`active_backup`](#active-backup) - Optional Object<br>Configuration parameter for active backup

<a id="devices"></a>&#x2022; [`devices`](#devices) - Optional List<br>Ethernet devices that will make up this bond

<a id="lacp"></a>&#x2022; [`lacp`](#lacp) - Optional String<br>LACP parameters. LACP parameters for the bond device

<a id="link-polling-interval"></a>&#x2022; [`link_polling_interval`](#link-polling-interval) - Optional Number<br>Link Polling Interval. Link polling interval in milliseconds

<a id="link-up-delay"></a>&#x2022; [`link_up_delay`](#link-up-delay) - Optional Number<br>Milliseconds wait before link is declared up

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>Bond Device Name. Name for the Bond. Ex 'bond0'

<a id="rate"></a>&#x2022; [`rate`](#rate) - Optional Number<br>Interval in seconds to transmit LACP packets

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="devices"></a>&#x2022; [`devices`](#devices) - Optional List<br>Configuration for all devices in the fleet. Examples of devices are - network interfaces, cameras, scanners etc. Configuration a device is applied on VER node if the VER node is member of this fleet and has an corresponding interface/device

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>Name of the device including the unit number (e.g. Eth0 or disk1). The name must match name of device in host-OS of node

<a id="network-device"></a>&#x2022; [`network_device`](#network-device) - Optional String<br>Represents physical network interface. The 'interface' reference points to a Network Interface object. Attributes such as Labels, MTU from Network Interface must be applied to the device

<a id="owner"></a>&#x2022; [`owner`](#owner) - Optional String  Defaults to `DEVICE_OWNER_INVALID`<br>Possible values are `DEVICE_OWNER_INVALID`, `DEVICE_OWNER_VER`, `DEVICE_OWNER_VK8S_WORK_LOAD`, `DEVICE_OWNER_HOST`<br>[Enum: DEVICE_OWNER_INVALID|DEVICE_OWNER_VER|DEVICE_OWNER_VK8S_WORK_LOAD|DEVICE_OWNER_HOST] Defines ownership for a device. Device owner is invalid Device is owned by VER pod.
Usually it will be network interface device or accelerator like crypto engine

<a id="interface"></a>&#x2022; [`interface`](#interface) - Optional List<br>Network Interface attributes for the device. User network interface configuration for this network device. Attributes like labels, MTU from the 'interface' are applied to corresponding interface in VER node If network interface refers to a virtual-network, the virtual-netowrk type must be

<a id="use"></a>&#x2022; [`use`](#use) - Optional String  Defaults to `NETWORK_INTERFACE_USE_REGULAR`<br>Possible values are `NETWORK_INTERFACE_USE_REGULAR`, `NETWORK_INTERFACE_USE_OUTSIDE`, `NETWORK_INTERFACE_USE_INSIDE`<br>[Enum: NETWORK_INTERFACE_USE_REGULAR|NETWORK_INTERFACE_USE_OUTSIDE|NETWORK_INTERFACE_USE_INSIDE] Defines how the device is used If networking device is owned by VER, it is
available for users to configure as required If networking device is owned by VER, it is included in bootstrap config and member of outside network. If networking device is owned by VER, it is included in bootstrap config

<a id="kind"></a>&#x2022; [`kind`](#kind) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="uid"></a>&#x2022; [`uid`](#uid) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. Route's) uid

<a id="feature-type"></a>&#x2022; [`feature_type`](#feature-type) - Optional String  Defaults to `UNLICENSED`<br>Possible values are `UNLICENSED`, `VGPU`, `VWS`, `VCS`<br>[Enum: UNLICENSED|VGPU|VWS|VCS] Set feature to be enabled Operate with a degraded vGPU performance Enable NVIDIA vGPU Enable NVIDIA RTX Virtual Workstation Enable NVIDIA Virtual Compute Server

<a id="server-address"></a>&#x2022; [`server_address`](#server-address) - Optional String<br>License Server Address. Set License Server Address

<a id="server-port"></a>&#x2022; [`server_port`](#server-port) - Optional Number<br>License Server Port Number. Set License Server port number

<a id="kind"></a>&#x2022; [`kind`](#kind) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="uid"></a>&#x2022; [`uid`](#uid) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. Route's) uid

<a id="interfaces"></a>&#x2022; [`interfaces`](#interfaces) - Optional List<br>Add all interfaces belonging to this fleet

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="disable-upgrade-drain"></a>&#x2022; [`disable_upgrade_drain`](#disable-upgrade-drain) - Optional Object<br>Configuration parameter for disable upgrade drain

<a id="enable-upgrade-drain"></a>&#x2022; [`enable_upgrade_drain`](#enable-upgrade-drain) - Optional String<br>Specify batch upgrade settings for worker nodes within a site

<a id="disable-vega-upgrade-mode"></a>&#x2022; [`disable_vega_upgrade_mode`](#disable-vega-upgrade-mode) - Optional Object<br>Configuration parameter for disable vega upgrade mode

<a id="drain-max-unavailable-node-count"></a>&#x2022; [`drain_max_unavailable_node_count`](#drain-max-unavailable-node-count) - Optional Number<br>Node Batch Size Count

<a id="drain-max-unavailable-node-percentage"></a>&#x2022; [`drain_max_unavailable_node_percentage`](#drain-max-unavailable-node-percentage) - Optional Number<br>Maximum percentage of nodes unavailable during upgrade draining

<a id="drain-node-timeout"></a>&#x2022; [`drain_node_timeout`](#drain-node-timeout) - Optional Number<br>Seconds to wait before initiating upgrade on the next set of nodes. Setting it to 0 will wait indefinitely for all services on nodes to be upgraded gracefully before proceeding to the next set of nodes. (Warning: It may block upgrade if services on a node cannot be gracefully upgraded. It is

<a id="enable-vega-upgrade-mode"></a>&#x2022; [`enable_vega_upgrade_mode`](#enable-vega-upgrade-mode) - Optional Object<br>Configuration parameter for enable vega upgrade mode

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="kind"></a>&#x2022; [`kind`](#kind) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="uid"></a>&#x2022; [`uid`](#uid) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. Route's) uid

<a id="kind"></a>&#x2022; [`kind`](#kind) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="uid"></a>&#x2022; [`uid`](#uid) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. Route's) uid

<a id="kind"></a>&#x2022; [`kind`](#kind) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="uid"></a>&#x2022; [`uid`](#uid) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. Route's) uid

<a id="perf-mode-l3-enhanced"></a>&#x2022; [`perf_mode_l3_enhanced`](#perf-mode-l3-enhanced) - Optional String<br>Configuration parameter for perf mode l3 enhanced

<a id="perf-mode-l7-enhanced"></a>&#x2022; [`perf_mode_l7_enhanced`](#perf-mode-l7-enhanced) - Optional String<br>Configuration parameter for perf mode l7 enhanced

<a id="jumbo"></a>&#x2022; [`jumbo`](#jumbo) - Optional Object<br>Enable this option

<a id="no-jumbo"></a>&#x2022; [`no_jumbo`](#no-jumbo) - Optional Object<br>Enable this option

<a id="jumbo-disabled"></a>&#x2022; [`jumbo_disabled`](#jumbo-disabled) - Optional Object<br>Enable this option

<a id="jumbo-enabled"></a>&#x2022; [`jumbo_enabled`](#jumbo-enabled) - Optional Object<br>Enable this option

<a id="sriov-interface"></a>&#x2022; [`sriov_interface`](#sriov-interface) - Optional List<br>Use custom SR-IOV interfaces Configuration

<a id="interface-name"></a>&#x2022; [`interface_name`](#interface-name) - Optional String<br>Name of physical interface. Name of SR-IOV physical interface

<a id="number-of-vfio-vfs"></a>&#x2022; [`number_of_vfio_vfs`](#number-of-vfio-vfs) - Optional Number<br>Number of virtual functions reserved for VNFs and DPDK-based CNFs

<a id="number-of-vfs"></a>&#x2022; [`number_of_vfs`](#number-of-vfs) - Optional Number<br>Total number of virtual functions. Total number of virtual functions

<a id="storage-classes"></a>&#x2022; [`storage_classes`](#storage-classes) - Optional List<br>List of Storage Classes. List of custom storage classes

<a id="advanced-storage-parameters"></a>&#x2022; [`advanced_storage_parameters`](#advanced-storage-parameters) - Optional Map<br>Advanced Parameters. Map of parameter name and string value

<a id="allow-volume-expansion"></a>&#x2022; [`allow_volume_expansion`](#allow-volume-expansion) - Optional Bool<br>Allow Volume Expansion. Allow volume expansion

<a id="custom-storage"></a>&#x2022; [`custom_storage`](#custom-storage) - Optional String<br>Custom Storage Class allows to insert Kubernetes storageclass definition which will be applied into given site

<a id="default-storage-class"></a>&#x2022; [`default_storage_class`](#default-storage-class) - Optional Bool<br>Make this storage class default storage class for the K8S cluster

<a id="description-spec"></a>&#x2022; [`description_spec`](#description-spec) - Optional String<br>Storage Class Description. Description for this storage class

<a id="hpe-storage"></a>&#x2022; [`hpe_storage`](#hpe-storage) - Optional String<br>Storage class Device configuration for HPE Storage

<a id="netapp-trident"></a>&#x2022; [`netapp_trident`](#netapp-trident) - Optional String<br>Storage class Device configuration for NetApp Trident

<a id="pure-service-orchestrator"></a>&#x2022; [`pure_service_orchestrator`](#pure-service-orchestrator) - Optional String<br>Storage class Device configuration for Pure Service Orchestrator

<a id="reclaim-policy"></a>&#x2022; [`reclaim_policy`](#reclaim-policy) - Optional String<br>Policy configuration for this feature

<a id="storage-class-name"></a>&#x2022; [`storage_class_name`](#storage-class-name) - Optional String<br>Name of the storage class as it will appear in K8S

<a id="storage-device"></a>&#x2022; [`storage_device`](#storage-device) - Optional String<br>Storage device that this class will use. The Device name defined at previous step

<a id="yaml"></a>&#x2022; [`yaml`](#yaml) - Optional String<br>Storage Class YAML. K8S YAML for StorageClass

<a id="allow-mutations"></a>&#x2022; [`allow_mutations`](#allow-mutations) - Optional String<br>Mutation can override specified parameters

<a id="allow-overrides"></a>&#x2022; [`allow_overrides`](#allow-overrides) - Optional String<br>AllowOverrides. PVC can override specified parameters

<a id="dedupe-enabled"></a>&#x2022; [`dedupe_enabled`](#dedupe-enabled) - Optional Bool<br>Indicates that the volume should enable deduplication

<a id="description-spec"></a>&#x2022; [`description_spec`](#description-spec) - Optional String<br>The SecretName parameter is used to identify name of secret to identify backend storage's auth information

<a id="destroy-on-delete"></a>&#x2022; [`destroy_on_delete`](#destroy-on-delete) - Optional Bool<br>Indicates the backing Nimble volume (including snapshots) should be destroyed when the PVC is deleted

<a id="encrypted"></a>&#x2022; [`encrypted`](#encrypted) - Optional Bool<br>Indicates that the volume should be encrypted

<a id="folder"></a>&#x2022; [`folder`](#folder) - Optional String<br>The name of the folder in which to place the volume

<a id="limit-iops"></a>&#x2022; [`limit_iops`](#limit-iops) - Optional String<br>LimitIops. The IOPS limit of the volume

<a id="limit-mbps"></a>&#x2022; [`limit_mbps`](#limit-mbps) - Optional String<br>LimitMbps. The IOPS limit of the volume

<a id="performance-policy"></a>&#x2022; [`performance_policy`](#performance-policy) - Optional String<br>Policy configuration for this feature

<a id="pool"></a>&#x2022; [`pool`](#pool) - Optional String<br>The name of the pool in which to place the volume

<a id="protection-template"></a>&#x2022; [`protection_template`](#protection-template) - Optional String<br>The name of the performance policy to assign to the volume

<a id="secret-name"></a>&#x2022; [`secret_name`](#secret-name) - Optional String<br>The SecretName parameter is used to identify name of secret to identify backend storage's auth information

<a id="secret-namespace"></a>&#x2022; [`secret_namespace`](#secret-namespace) - Optional String<br>The SecretNamespace parameter is used to identify name of namespace where secret resides

<a id="sync-on-detach"></a>&#x2022; [`sync_on_detach`](#sync-on-detach) - Optional Bool<br>Indicates that a snapshot of the volume should be synced to the replication partner each time it is detached from a node

<a id="thick"></a>&#x2022; [`thick`](#thick) - Optional Bool<br>Indicates that the volume should be thick provisioned

<a id="selector"></a>&#x2022; [`selector`](#selector) - Optional String<br>Using the Selector field, each StorageClass calls out which virtual pool(s) may be used to host a volume. The volume will have the aspects defined in the chosen virtual pool

<a id="storage-pools"></a>&#x2022; [`storage_pools`](#storage-pools) - Optional String<br>The storagePools parameter is used to further restrict the set of pools that match any specified attributes

<a id="backend"></a>&#x2022; [`backend`](#backend) - Optional String<br>Possible values are `block`, `file`<br>[Enum: block|file] Defines type of Pure storage backend block or file. The volume will have the aspects defined in the chosen virtual pool

<a id="bandwidth-limit"></a>&#x2022; [`bandwidth_limit`](#bandwidth-limit) - Optional String<br>It must be between 1 MB/s and 512 GB/s. Enter the size as a number (bytes must be multiple of 512) or number with a single character unit symbol. Valid unit symbols are K, M, G, representing KiB, MiB, and GiB

<a id="iops-limit"></a>&#x2022; [`iops_limit`](#iops-limit) - Optional Number<br>Enable IOPS limitation. It must be between 100 and 100 million. If value is 0, IOPS limit is not defined

<a id="storage-devices"></a>&#x2022; [`storage_devices`](#storage-devices) - Optional List<br>List of Storage Devices. List of custom storage devices

<a id="advanced-advanced-parameters"></a>&#x2022; [`advanced_advanced_parameters`](#advanced-advanced-parameters) - Optional Map<br>Advanced Parameters. Map of parameter name and string value

<a id="custom-storage"></a>&#x2022; [`custom_storage`](#custom-storage) - Optional Object<br>Configuration parameter for custom storage

<a id="hpe-storage"></a>&#x2022; [`hpe_storage`](#hpe-storage) - Optional String<br>Configuration parameter for hpe storage

<a id="netapp-trident"></a>&#x2022; [`netapp_trident`](#netapp-trident) - Optional String<br>Device configuration for NetApp Trident Storage

<a id="pure-service-orchestrator"></a>&#x2022; [`pure_service_orchestrator`](#pure-service-orchestrator) - Optional String<br>Device configuration for Pure Storage Service Orchestrator

<a id="storage-device"></a>&#x2022; [`storage_device`](#storage-device) - Optional String<br>Storage Device. Storage device and device unit

<a id="api-server-port"></a>&#x2022; [`api_server_port`](#api-server-port) - Optional Number<br>Storage server Port. Enter Storage Server Port

<a id="iscsi-chap-password"></a>&#x2022; [`iscsi_chap_password`](#iscsi-chap-password) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="iscsi-chap-user"></a>&#x2022; [`iscsi_chap_user`](#iscsi-chap-user) - Optional String<br>Chap Username to connect to the HPE storage

<a id="password"></a>&#x2022; [`password`](#password) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="storage-server-ip-address"></a>&#x2022; [`storage_server_ip_address`](#storage-server-ip-address) - Optional String<br>Storage Server IP address. Enter storage server IP address

<a id="storage-server-name"></a>&#x2022; [`storage_server_name`](#storage-server-name) - Optional String<br>Storage Server Name. Enter storage server Name

<a id="username"></a>&#x2022; [`username`](#username) - Optional String<br>Username to connect to the HPE storage management IP

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="netapp-backend-ontap-nas"></a>&#x2022; [`netapp_backend_ontap_nas`](#netapp-backend-ontap-nas) - Optional String<br>Configuration of storage backend for NetApp ONTAP NAS

<a id="netapp-backend-ontap-san"></a>&#x2022; [`netapp_backend_ontap_san`](#netapp-backend-ontap-san) - Optional String<br>Configuration of storage backend for NetApp ONTAP SAN

<a id="auto-export-cidrs"></a>&#x2022; [`auto_export_cidrs`](#auto-export-cidrs) - Optional String<br>List of IPv4 prefixes that represent an endpoint

<a id="auto-export-policy"></a>&#x2022; [`auto_export_policy`](#auto-export-policy) - Optional Bool<br>Policy configuration for this feature

<a id="backend-name"></a>&#x2022; [`backend_name`](#backend-name) - Optional String<br>Configuration of Backend Name. Driver is name + '_' + dataLIF

<a id="client-certificate"></a>&#x2022; [`client_certificate`](#client-certificate) - Optional String<br>Please Enter Base64-encoded value of client certificate. Used for certificate-based auth

<a id="client-private-key"></a>&#x2022; [`client_private_key`](#client-private-key) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="data-lif-dns-name"></a>&#x2022; [`data_lif_dns_name`](#data-lif-dns-name) - Optional String<br>Backend Data LIF IP Address's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="data-lif-ip"></a>&#x2022; [`data_lif_ip`](#data-lif-ip) - Optional String<br>Backend Data LIF IP Address is reachable at the given IP address

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>List of labels for Storage Device used in NetApp ONTAP. It is used for storage class selection

<a id="limit-aggregate-usage"></a>&#x2022; [`limit_aggregate_usage`](#limit-aggregate-usage) - Optional String<br>Fail provisioning if usage is above this percentage. Not enforced by default

<a id="limit-volume-size"></a>&#x2022; [`limit_volume_size`](#limit-volume-size) - Optional String<br>Fail provisioning if requested volume size is above this value. Not enforced by default

<a id="management-lif-dns-name"></a>&#x2022; [`management_lif_dns_name`](#management-lif-dns-name) - Optional String<br>Backend Management LIF IP Address's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="management-lif-ip"></a>&#x2022; [`management_lif_ip`](#management-lif-ip) - Optional String<br>Backend Management LIF IP Address is reachable at the given IP address

<a id="nfs-mount-options"></a>&#x2022; [`nfs_mount_options`](#nfs-mount-options) - Optional String<br>Comma-separated list of NFS mount OPTIONS. Not enforced by default

<a id="password"></a>&#x2022; [`password`](#password) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="region"></a>&#x2022; [`region`](#region) - Optional String<br>Backend Region. Virtual Pool Region

<a id="storage"></a>&#x2022; [`storage`](#storage) - Optional List<br>List of Virtual Storage Pool definitions which are referred back by Storage Class label match selection

<a id="storage-driver-name"></a>&#x2022; [`storage_driver_name`](#storage-driver-name) - Optional String<br>Possible values are `ontap-nas`, `ontap-nas-economy`, `ontap-nas-flexgroup`<br>[Enum: ontap-nas|ontap-nas-economy|ontap-nas-flexgroup] Storage Backend Driver. Configuration of Backend Name

<a id="storage-prefix"></a>&#x2022; [`storage_prefix`](#storage-prefix) - Optional String<br>Prefix used when provisioning new volumes in the SVM. Once set this cannot be updated

<a id="svm"></a>&#x2022; [`svm`](#svm) - Optional String<br>Storage virtual machine to use. Derived if an SVM managementLIF is specified

<a id="trusted-ca-certificate"></a>&#x2022; [`trusted_ca_certificate`](#trusted-ca-certificate) - Optional String<br>Please Enter Base64-encoded value of trusted CA certificate. Optional. Used for certificate-based auth

<a id="username"></a>&#x2022; [`username`](#username) - Optional String<br>Username. Username to connect to the cluster/SVM

<a id="volume-defaults"></a>&#x2022; [`volume_defaults`](#volume-defaults) - Optional String<br>It controls how each volume is provisioned by default using these OPTIONS in a special section of the configuration

<a id="prefixes"></a>&#x2022; [`prefixes`](#prefixes) - Optional List<br>List of IPv4 prefixes that represent an endpoint

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>List of labels for Storage Device used in NetApp ONTAP. It is used for storage class label match selection

<a id="volume-defaults"></a>&#x2022; [`volume_defaults`](#volume-defaults) - Optional String<br>It controls how each volume is provisioned by default using these OPTIONS in a special section of the configuration

<a id="zone"></a>&#x2022; [`zone`](#zone) - Optional String<br>Virtual Pool Zone. Virtual Storage Pool zone definition

<a id="adaptive-qos-policy"></a>&#x2022; [`adaptive_qos_policy`](#adaptive-qos-policy) - Optional String<br>Policy configuration for this feature

<a id="encryption"></a>&#x2022; [`encryption`](#encryption) - Optional Bool<br>Enable Encryption. Enable NetApp volume encryption

<a id="export-policy"></a>&#x2022; [`export_policy`](#export-policy) - Optional String<br>Policy configuration for this feature

<a id="no-qos"></a>&#x2022; [`no_qos`](#no-qos) - Optional Object<br>Enable this option

<a id="qos-policy"></a>&#x2022; [`qos_policy`](#qos-policy) - Optional String<br>Policy configuration for this feature

<a id="security-style"></a>&#x2022; [`security_style`](#security-style) - Optional String<br>Security Style. Security style for new volumes

<a id="snapshot-dir"></a>&#x2022; [`snapshot_dir`](#snapshot-dir) - Optional Bool<br>Access to Snapshot Directory. Access to the .snapshot directory

<a id="snapshot-policy"></a>&#x2022; [`snapshot_policy`](#snapshot-policy) - Optional String<br>Policy configuration for this feature

<a id="snapshot-reserve"></a>&#x2022; [`snapshot_reserve`](#snapshot-reserve) - Optional String<br>Percentage of volume reserved for snapshots. '0' if snapshot policy is 'none', else ''

<a id="space-reserve"></a>&#x2022; [`space_reserve`](#space-reserve) - Optional String<br>Possible values are `none`, `thick`<br>[Enum: none|thick] Space reservation mode; “none” (thin) or “volume” (thick)

<a id="split-on-clone"></a>&#x2022; [`split_on_clone`](#split-on-clone) - Optional Bool<br>Split a clone from its parent upon creation

<a id="tiering-policy"></a>&#x2022; [`tiering_policy`](#tiering-policy) - Optional String<br>Policy configuration for this feature

<a id="unix-permissions"></a>&#x2022; [`unix_permissions`](#unix-permissions) - Optional Number<br>Unix permission mode for new volumes. All allowed 777

<a id="adaptive-qos-policy"></a>&#x2022; [`adaptive_qos_policy`](#adaptive-qos-policy) - Optional String<br>Policy configuration for this feature

<a id="encryption"></a>&#x2022; [`encryption`](#encryption) - Optional Bool<br>Enable Encryption. Enable NetApp volume encryption

<a id="export-policy"></a>&#x2022; [`export_policy`](#export-policy) - Optional String<br>Policy configuration for this feature

<a id="no-qos"></a>&#x2022; [`no_qos`](#no-qos) - Optional Object<br>Enable this option

<a id="qos-policy"></a>&#x2022; [`qos_policy`](#qos-policy) - Optional String<br>Policy configuration for this feature

<a id="security-style"></a>&#x2022; [`security_style`](#security-style) - Optional String<br>Security Style. Security style for new volumes

<a id="snapshot-dir"></a>&#x2022; [`snapshot_dir`](#snapshot-dir) - Optional Bool<br>Access to Snapshot Directory. Access to the .snapshot directory

<a id="snapshot-policy"></a>&#x2022; [`snapshot_policy`](#snapshot-policy) - Optional String<br>Policy configuration for this feature

<a id="snapshot-reserve"></a>&#x2022; [`snapshot_reserve`](#snapshot-reserve) - Optional String<br>Percentage of volume reserved for snapshots. '0' if snapshot policy is 'none', else ''

<a id="space-reserve"></a>&#x2022; [`space_reserve`](#space-reserve) - Optional String<br>Possible values are `none`, `thick`<br>[Enum: none|thick] Space reservation mode; “none” (thin) or “volume” (thick)

<a id="split-on-clone"></a>&#x2022; [`split_on_clone`](#split-on-clone) - Optional Bool<br>Split a clone from its parent upon creation

<a id="tiering-policy"></a>&#x2022; [`tiering_policy`](#tiering-policy) - Optional String<br>Policy configuration for this feature

<a id="unix-permissions"></a>&#x2022; [`unix_permissions`](#unix-permissions) - Optional Number<br>Unix permission mode for new volumes. All allowed 777

<a id="client-certificate"></a>&#x2022; [`client_certificate`](#client-certificate) - Optional String<br>Please Enter Base64-encoded value of client certificate. Used for certificate-based auth

<a id="client-private-key"></a>&#x2022; [`client_private_key`](#client-private-key) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="data-lif-dns-name"></a>&#x2022; [`data_lif_dns_name`](#data-lif-dns-name) - Optional String<br>Backend Data LIF IP Address's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="data-lif-ip"></a>&#x2022; [`data_lif_ip`](#data-lif-ip) - Optional String<br>Backend Data LIF IP Address is reachable at the given IP address

<a id="igroup-name"></a>&#x2022; [`igroup_name`](#igroup-name) - Optional String<br>Name of the igroup for SAN volumes to use

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>List of labels for Storage Device used in NetApp ONTAP. It is used for storage class selection

<a id="limit-aggregate-usage"></a>&#x2022; [`limit_aggregate_usage`](#limit-aggregate-usage) - Optional Number<br>Fail provisioning if usage is above this percentage. Not enforced by default

<a id="limit-volume-size"></a>&#x2022; [`limit_volume_size`](#limit-volume-size) - Optional Number<br>Fail provisioning if requested volume size in GBi is above this value. Not enforced by default

<a id="management-lif-dns-name"></a>&#x2022; [`management_lif_dns_name`](#management-lif-dns-name) - Optional String<br>Backend Management LIF IP Address's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="management-lif-ip"></a>&#x2022; [`management_lif_ip`](#management-lif-ip) - Optional String<br>Backend Management LIF IP Address is reachable at the given IP address

<a id="no-chap"></a>&#x2022; [`no_chap`](#no-chap) - Optional Object<br>Enable this option

<a id="password"></a>&#x2022; [`password`](#password) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="region"></a>&#x2022; [`region`](#region) - Optional String<br>Backend Region. Virtual Pool Region

<a id="storage"></a>&#x2022; [`storage`](#storage) - Optional List<br>List of Virtual Storage Pool definitions which are referred back by Storage Class label match selection

<a id="storage-driver-name"></a>&#x2022; [`storage_driver_name`](#storage-driver-name) - Optional String<br>Possible values are `ontap-san`, `ontap-san-economy`, `ontap-nas-flexgroup`<br>[Enum: ontap-san|ontap-san-economy|ontap-nas-flexgroup] Storage Backend Driver. Configuration of Backend Name

<a id="storage-prefix"></a>&#x2022; [`storage_prefix`](#storage-prefix) - Optional String<br>Prefix used when provisioning new volumes in the SVM. Once set this cannot be updated

<a id="svm"></a>&#x2022; [`svm`](#svm) - Optional String<br>Storage virtual machine to use. Derived if an SVM managementLIF is specified

<a id="trusted-ca-certificate"></a>&#x2022; [`trusted_ca_certificate`](#trusted-ca-certificate) - Optional String<br>Please Enter Base64-encoded value of trusted CA certificate. Optional. Used for certificate-based auth

<a id="use-chap"></a>&#x2022; [`use_chap`](#use-chap) - Optional String<br>Device NetApp Backend ONTAP SAN CHAP configuration OPTIONS for enabled CHAP

<a id="username"></a>&#x2022; [`username`](#username) - Optional String<br>Username. Username to connect to the cluster/SVM

<a id="volume-defaults"></a>&#x2022; [`volume_defaults`](#volume-defaults) - Optional String<br>It controls how each volume is provisioned by default using these OPTIONS in a special section of the configuration

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>List of labels for Storage Device used in NetApp ONTAP. It is used for storage class label match selection

<a id="volume-defaults"></a>&#x2022; [`volume_defaults`](#volume-defaults) - Optional String<br>It controls how each volume is provisioned by default using these OPTIONS in a special section of the configuration

<a id="zone"></a>&#x2022; [`zone`](#zone) - Optional String<br>Virtual Pool Zone. Virtual Storage Pool zone definition

<a id="adaptive-qos-policy"></a>&#x2022; [`adaptive_qos_policy`](#adaptive-qos-policy) - Optional String<br>Policy configuration for this feature

<a id="encryption"></a>&#x2022; [`encryption`](#encryption) - Optional Bool<br>Enable Encryption. Enable NetApp volume encryption

<a id="export-policy"></a>&#x2022; [`export_policy`](#export-policy) - Optional String<br>Policy configuration for this feature

<a id="no-qos"></a>&#x2022; [`no_qos`](#no-qos) - Optional Object<br>Enable this option

<a id="qos-policy"></a>&#x2022; [`qos_policy`](#qos-policy) - Optional String<br>Policy configuration for this feature

<a id="security-style"></a>&#x2022; [`security_style`](#security-style) - Optional String<br>Security Style. Security style for new volumes

<a id="snapshot-dir"></a>&#x2022; [`snapshot_dir`](#snapshot-dir) - Optional Bool<br>Access to Snapshot Directory. Access to the .snapshot directory

<a id="snapshot-policy"></a>&#x2022; [`snapshot_policy`](#snapshot-policy) - Optional String<br>Policy configuration for this feature

<a id="snapshot-reserve"></a>&#x2022; [`snapshot_reserve`](#snapshot-reserve) - Optional String<br>Percentage of volume reserved for snapshots. '0' if snapshot policy is 'none', else ''

<a id="space-reserve"></a>&#x2022; [`space_reserve`](#space-reserve) - Optional String<br>Possible values are `none`, `thick`<br>[Enum: none|thick] Space reservation mode; “none” (thin) or “volume” (thick)

<a id="split-on-clone"></a>&#x2022; [`split_on_clone`](#split-on-clone) - Optional Bool<br>Split a clone from its parent upon creation

<a id="tiering-policy"></a>&#x2022; [`tiering_policy`](#tiering-policy) - Optional String<br>Policy configuration for this feature

<a id="unix-permissions"></a>&#x2022; [`unix_permissions`](#unix-permissions) - Optional Number<br>Unix permission mode for new volumes. All allowed 777

<a id="chap-initiator-secret"></a>&#x2022; [`chap_initiator_secret`](#chap-initiator-secret) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="chap-target-initiator-secret"></a>&#x2022; [`chap_target_initiator_secret`](#chap-target-initiator-secret) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="chap-target-username"></a>&#x2022; [`chap_target_username`](#chap-target-username) - Optional String<br>Target username. Required if useCHAP=true

<a id="chap-username"></a>&#x2022; [`chap_username`](#chap-username) - Optional String<br>Inbound username. Required if useCHAP=true

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="adaptive-qos-policy"></a>&#x2022; [`adaptive_qos_policy`](#adaptive-qos-policy) - Optional String<br>Policy configuration for this feature

<a id="encryption"></a>&#x2022; [`encryption`](#encryption) - Optional Bool<br>Enable Encryption. Enable NetApp volume encryption

<a id="export-policy"></a>&#x2022; [`export_policy`](#export-policy) - Optional String<br>Policy configuration for this feature

<a id="no-qos"></a>&#x2022; [`no_qos`](#no-qos) - Optional Object<br>Enable this option

<a id="qos-policy"></a>&#x2022; [`qos_policy`](#qos-policy) - Optional String<br>Policy configuration for this feature

<a id="security-style"></a>&#x2022; [`security_style`](#security-style) - Optional String<br>Security Style. Security style for new volumes

<a id="snapshot-dir"></a>&#x2022; [`snapshot_dir`](#snapshot-dir) - Optional Bool<br>Access to Snapshot Directory. Access to the .snapshot directory

<a id="snapshot-policy"></a>&#x2022; [`snapshot_policy`](#snapshot-policy) - Optional String<br>Policy configuration for this feature

<a id="snapshot-reserve"></a>&#x2022; [`snapshot_reserve`](#snapshot-reserve) - Optional String<br>Percentage of volume reserved for snapshots. '0' if snapshot policy is 'none', else ''

<a id="space-reserve"></a>&#x2022; [`space_reserve`](#space-reserve) - Optional String<br>Possible values are `none`, `thick`<br>[Enum: none|thick] Space reservation mode; “none” (thin) or “volume” (thick)

<a id="split-on-clone"></a>&#x2022; [`split_on_clone`](#split-on-clone) - Optional Bool<br>Split a clone from its parent upon creation

<a id="tiering-policy"></a>&#x2022; [`tiering_policy`](#tiering-policy) - Optional String<br>Policy configuration for this feature

<a id="unix-permissions"></a>&#x2022; [`unix_permissions`](#unix-permissions) - Optional Number<br>Unix permission mode for new volumes. All allowed 777

<a id="arrays"></a>&#x2022; [`arrays`](#arrays) - Optional String<br>Arrays Configuration. Device configuration for PSO Arrays

<a id="cluster-id"></a>&#x2022; [`cluster_id`](#cluster-id) - Optional String<br>ClusterID is added as a prefix for all volumes created by this PSO installation. ClusterID is also used to identify the volumes used by the datastore, pso-db. ClusterID MUST BE UNIQUE for multiple K8S clusters running on top of the same storage arrays

<a id="enable-storage-topology"></a>&#x2022; [`enable_storage_topology`](#enable-storage-topology) - Optional Bool<br>Option is to enable/disable the csi topology feature for pso-csi

<a id="enable-strict-topology"></a>&#x2022; [`enable_strict_topology`](#enable-strict-topology) - Optional Bool<br>Option is to enable/disable the strict csi topology feature for pso-csi

<a id="flash-array"></a>&#x2022; [`flash_array`](#flash-array) - Optional String<br>Specify what storage flash arrays should be managed the plugin

<a id="flash-blade"></a>&#x2022; [`flash_blade`](#flash-blade) - Optional String<br>Specify what storage flash blades should be managed the plugin

<a id="default-fs-opt"></a>&#x2022; [`default_fs_opt`](#default-fs-opt) - Optional String<br>Block volume default mkfs OPTIONS. Not recommended to change!

<a id="default-fs-type"></a>&#x2022; [`default_fs_type`](#default-fs-type) - Optional String<br>Possible values are `xfs`, `ext4`<br>[Enum: xfs|ext4] Block volume default filesystem type. Not recommended to change!

<a id="default-mount-opts"></a>&#x2022; [`default_mount_opts`](#default-mount-opts) - Optional List<br>Block volume default filesystem mount OPTIONS. Not recommended to change!

<a id="disable-preempt-attachments"></a>&#x2022; [`disable_preempt_attachments`](#disable-preempt-attachments) - Optional Bool<br>Disable Preempt Attachments. Enable/Disable attachment preemption!

<a id="flash-arrays"></a>&#x2022; [`flash_arrays`](#flash-arrays) - Optional List<br>For FlashArrays you must set the 'mgmt_endpoint' and 'api_token'

<a id="iscsi-login-timeout"></a>&#x2022; [`iscsi_login_timeout`](#iscsi-login-timeout) - Optional Number<br>iSCSI login timeout in seconds. Not recommended to change!

<a id="san-type"></a>&#x2022; [`san_type`](#san-type) - Optional String<br>Possible values are `iSCSI`, `FC`<br>[Enum: iSCSI|FC] Block volume access protocol, either iSCSI or FC

<a id="api-token"></a>&#x2022; [`api_token`](#api-token) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>Specifies labels optional, and can be any key-value pair for use with the PSO 'fleet' provisioner

<a id="mgmt-dns-name"></a>&#x2022; [`mgmt_dns_name`](#mgmt-dns-name) - Optional String<br>Management Endpoint's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="mgmt-ip"></a>&#x2022; [`mgmt_ip`](#mgmt-ip) - Optional String<br>Management Endpoint is reachable at the given IP address

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="enable-snapshot-directory"></a>&#x2022; [`enable_snapshot_directory`](#enable-snapshot-directory) - Optional Bool<br>Enable Snapshot Directory. Enable/Disable FlashBlade snapshots

<a id="export-rules"></a>&#x2022; [`export_rules`](#export-rules) - Optional String<br>NFS Export Rules. NFS Export rules

<a id="flash-blades"></a>&#x2022; [`flash_blades`](#flash-blades) - Optional List<br>For FlashBlades you must set the 'mgmt_endpoint', 'api_token' and nfs_endpoint

<a id="api-token"></a>&#x2022; [`api_token`](#api-token) - Optional String<br>SecretType is used in an object to indicate a sensitive/confidential field

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional Map<br>Specifies labels optional, and can be any key-value pair for use with the PSO 'fleet' provisioner

<a id="mgmt-dns-name"></a>&#x2022; [`mgmt_dns_name`](#mgmt-dns-name) - Optional String<br>Management Endpoint's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="mgmt-ip"></a>&#x2022; [`mgmt_ip`](#mgmt-ip) - Optional String<br>Management Endpoint is reachable at the given IP address

<a id="nfs-endpoint-dns-name"></a>&#x2022; [`nfs_endpoint_dns_name`](#nfs-endpoint-dns-name) - Optional String<br>Endpoint's IP address is discovered using DNS name resolution. The name given here is fully qualified domain name

<a id="nfs-endpoint-ip"></a>&#x2022; [`nfs_endpoint_ip`](#nfs-endpoint-ip) - Optional String<br>Endpoint is reachable at the given IP address

<a id="blindfold-secret-info"></a>&#x2022; [`blindfold_secret_info`](#blindfold-secret-info) - Optional String<br>BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management

<a id="clear-secret-info"></a>&#x2022; [`clear_secret_info`](#clear-secret-info) - Optional String<br>ClearSecretInfoType specifies information about the Secret that is not encrypted

<a id="decryption-provider"></a>&#x2022; [`decryption_provider`](#decryption-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the backend Secret Management service

<a id="location"></a>&#x2022; [`location`](#location) - Optional String<br>Location is the uri_ref. It could be in URL format for string:/// Or it could be a path if the store provider is an HTTP/HTTPS location

<a id="store-provider"></a>&#x2022; [`store_provider`](#store-provider) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="provider-ref"></a>&#x2022; [`provider_ref`](#provider-ref) - Optional String<br>Name of the Secret Management Access object that contains information about the store to GET encrypted bytes This field needs to be provided only if the URL scheme is not string:///

<a id="url"></a>&#x2022; [`url`](#url) - Optional String<br>URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will GET Secret bytes after Base64 decoding

<a id="interfaces"></a>&#x2022; [`interfaces`](#interfaces) - Optional List<br>Add all interfaces belonging to this fleet

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="storage-routes"></a>&#x2022; [`storage_routes`](#storage-routes) - Optional List<br>List of Static Routes. List of storage static routes

<a id="attrs"></a>&#x2022; [`attrs`](#attrs) - Optional List  Defaults to `ROUTE_ATTR_NO_OP`<br>Possible values are `ROUTE_ATTR_NO_OP`, `ROUTE_ATTR_ADVERTISE`, `ROUTE_ATTR_INSTALL_HOST`, `ROUTE_ATTR_INSTALL_FORWARDING`, `ROUTE_ATTR_MERGE_ONLY`<br>[Enum: ROUTE_ATTR_NO_OP|ROUTE_ATTR_ADVERTISE|ROUTE_ATTR_INSTALL_HOST|ROUTE_ATTR_INSTALL_FORWARDING|ROUTE_ATTR_MERGE_ONLY] List of route attributes
associated with the static route

<a id="labels"></a>&#x2022; [`labels`](#labels) - Optional String<br>Add Labels for this Static Route, these labels can be used in network policy

<a id="nexthop"></a>&#x2022; [`nexthop`](#nexthop) - Optional String<br>Nexthop. Identifies the next-hop for a route

<a id="subnets"></a>&#x2022; [`subnets`](#subnets) - Optional List<br>Subnets. List of route prefixes

<a id="interface"></a>&#x2022; [`interface`](#interface) - Optional List<br>Nexthop is network interface when type is 'Network-Interface'

<a id="nexthop-address"></a>&#x2022; [`nexthop_address`](#nexthop-address) - Optional String<br>IP Address used to specify an IPv4 or IPv6 address

<a id="type"></a>&#x2022; [`type`](#type) - Optional String  Defaults to `NEXT_HOP_DEFAULT_GATEWAY`<br>Possible values are `NEXT_HOP_DEFAULT_GATEWAY`, `NEXT_HOP_USE_CONFIGURED`, `NEXT_HOP_NETWORK_INTERFACE`<br>[Enum: NEXT_HOP_DEFAULT_GATEWAY|NEXT_HOP_USE_CONFIGURED|NEXT_HOP_NETWORK_INTERFACE] Defines types of next-hop Use default gateway on the local interface as gateway for route. Assumes there
is only one local interface on the virtual network. Use the specified address as nexthop Use the network interface as nexthop Discard nexthop, used when attr type is Advertise Used in VoltADN

<a id="kind"></a>&#x2022; [`kind`](#kind) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

<a id="uid"></a>&#x2022; [`uid`](#uid) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. Route's) uid

<a id="ipv4"></a>&#x2022; [`ipv4`](#ipv4) - Optional String<br>IPv4 address in dotted decimal notation (e.g., 192.0.2.1)

<a id="ipv6"></a>&#x2022; [`ipv6`](#ipv6) - Optional String<br>IPv6 Address specified as hexadecimal numbers separated by ':'

<a id="addr"></a>&#x2022; [`addr`](#addr) - Optional String<br>IPv4 Address in string form with dot-decimal notation

<a id="addr"></a>&#x2022; [`addr`](#addr) - Optional String<br>IPv6 Address in form of string. IPv6 address must be specified as hexadecimal numbers separated by ':' The address can be compacted by suppressing zeros e.g. '2001:db8:0:0:0:0:2:1' becomes '2001:db8::2:1' or '2001:db8:0:0:0:2:0:0' becomes '2001:db8::2::'

<a id="ipv4"></a>&#x2022; [`ipv4`](#ipv4) - Optional String<br>IPv4 subnets specified as prefix and prefix-length. Prefix length must be <= 32

<a id="ipv6"></a>&#x2022; [`ipv6`](#ipv6) - Optional String<br>IPv6 subnets specified as prefix and prefix-length. Prefix-legnth must be <= 128

<a id="plen"></a>&#x2022; [`plen`](#plen) - Optional Number<br>Prefix-length of the IPv4 subnet. Must be <= 32

<a id="prefix"></a>&#x2022; [`prefix`](#prefix) - Optional String<br>Prefix part of the IPv4 subnet in string form with dot-decimal notation

<a id="plen"></a>&#x2022; [`plen`](#plen) - Optional Number<br>Prefix length of the IPv6 subnet. Must be <= 128

<a id="prefix"></a>&#x2022; [`prefix`](#prefix) - Optional String<br>Prefix part of the IPv6 subnet given in form of string. IPv6 address must be specified as hexadecimal numbers separated by ':' e.g. '2001:db8:0:0:0:2:0:0' The address can be compacted by suppressing zeros e.g. '2001:db8::2::'

<a id="name"></a>&#x2022; [`name`](#name) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. Route's) name

<a id="namespace"></a>&#x2022; [`namespace`](#namespace) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. Route's) namespace

<a id="tenant"></a>&#x2022; [`tenant`](#tenant) - Optional String<br>When a configuration object(e.g. Virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. Route's) tenant

---

## Common Types

The following type definitions are used throughout this resource. See the full definition here rather than repeated inline.

### Object Reference {#common-object-reference}

Object references establish a direct reference from one configuration object to another in F5 Distributed Cloud. References use the format `tenant/namespace/name`.

| Field | Type | Description |
| ----- | ---- | ----------- |
| `name` | String | Name of the referenced object |
| `namespace` | String | Namespace containing the referenced object |
| `tenant` | String | Tenant of the referenced object (system-managed) |

### Transformers {#common-transformers}

Transformers apply transformations to input values before matching. Multiple transformers can be applied in order.

| Value | Description |
| ----- | ----------- |
| `LOWER_CASE` | Convert to lowercase |
| `UPPER_CASE` | Convert to uppercase |
| `BASE64_DECODE` | Decodebase64 content |
| `NORMALIZE_PATH` | Normalize URL path |
| `REMOVE_WHITESPACE` | Remove whitespace characters |
| `URL_DECODE` | Decode URL-encoded characters |
| `TRIM_LEFT` | Trim leading whitespace |
| `TRIM_RIGHT` | Trim trailing whitespace |
| `TRIM` | Trim both leading and trailing whitespace |

### HTTP Methods {#common-http-methods}

HTTP methods used for request matching.

| Value | Description |
| ----- | ----------- |
| `ANY` | Match any HTTP method |
| `GET` | HTTP GET request |
| `HEAD` | HTTP HEAD request |
| `POST` | HTTP POST request |
| `PUT` | HTTP PUT request |
| `DELETE` | HTTP DELETE request |
| `CONNECT` | HTTP CONNECT request |
| `OPTIONS` | HTTP OPTIONS request |
| `TRACE` | HTTP TRACE request |
| `PATCH` | HTTP PATCH request |
| `COPY` | HTTP COPY request (WebDAV) |

### TLS Fingerprints {#common-tls-fingerprints}

TLS fingerprint categories for malicious client detection.

| Value | Description |
| ----- | ----------- |
| `TLS_FINGERPRINT_NONE` | No fingerprint matching |
| `ANY_MALICIOUS_FINGERPRINT` | Match any known malicious fingerprint |
| `ADWARE` | Adware-associated fingerprints |
| `DRIDEX` | Dridex malware fingerprints |
| `GOOTKIT` | Gootkit malware fingerprints |
| `RANSOMWARE` | Ransomware-associated fingerprints |
| `TRICKBOT` | Trickbot malware fingerprints |

### IP Threat Categories {#common-ip-threat-categories}

IP address threat categories for security filtering.

| Value | Description |
| ----- | ----------- |
| `SPAM_SOURCES` | Known spam sources |
| `WINDOWS_EXPLOITS` | Windows exploit sources |
| `WEB_ATTACKS` | Web attack sources |
| `BOTNETS` | Known botnet IPs |
| `SCANNERS` | Network scanner IPs |
| `REPUTATION` | Poor reputation IPs |
| `PHISHING` | Phishing-related IPs |
| `PROXY` | Anonymous proxy IPs |
| `MOBILE_THREATS` | Mobile threat sources |
| `TOR_PROXY` | Tor exit nodes |
| `DENIAL_OF_SERVICE` | DoS attack sources |
| `NETWORK` | Known bad network ranges |
