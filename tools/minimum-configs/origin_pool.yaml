apiVersion: v1
kind: origin_pool
metadata:
  name: backend-pool
  namespace: demo-app
spec:
  origin_servers:
    - public_name:
        dns_name: backend1.example.com
    - public_name:
        dns_name: backend2.example.com
  port: 8080
